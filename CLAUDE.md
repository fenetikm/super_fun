# Super Fun

Go lang CLI with a TUI that simulates a support tool for managing many devices across a network.

## Commands
- `go build ./...` — build all packages
- `go test ./...` — run all tests
- `go run .` — run the main package

## Conventions
- Incremental development: keep changes small and focused
- Write tests alongside new code
